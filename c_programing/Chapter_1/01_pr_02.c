#include <stdio.h>

int main()
{
    int radius = 5;
    float pi = 3.14;
    printf("The area of circle is : %f \n", pi * radius * radius);
    int height = 6;
    printf("Volume of this cylinder if %f \n", pi * radius * radius * height);
    return 0;
}